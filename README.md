# Production-Grade EpicBook: Terraform + Ansible Roles: Deployment on AWS

Deployment of the EpicBook web application on a cloud VM provisioned with Terraform (Azure or AWS — pick one) and configured through reusable Ansible roles (common, nginx, epicbook) orchestrated by one playbook, using group variables, templates, and handlers, with a verified idempotent second run.
